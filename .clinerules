# Lucky Flutter App - Cline Agent 专属核心规则

## 0. 最高指令 (CRITICAL)
- 严禁猜测！修改任何代码前，必须先 `read_file` 读取目标文件。
- [cite_start]严禁韩文！仅限中英文回复与代码注释 。
- [cite_start]强制 FVM！所有 flutter/dart 命令必须加前缀，例如：`fvm flutter ...` [cite: 2]。
- [cite_start]保护内存！宿主机为 8GB 内存，绝对禁止执行 `build_runner watch`，模型变更后仅允许执行单次构建：`fvm dart run build_runner build --delete-conflicting-outputs` 。

## 1. 引导与任务追踪
- [cite_start]每次对话开始，立即读取 `.github/copilot-instructions.md` 和 `docs/AI_QUICK_START.md` 获取上下文 。
- [cite_start]任务完成时，必须主动更新 `.github/copilot-instructions.md` 中的 `[ ]` 状态 。

## 2. 成本控制 (防 Token 燃烧机制)
- 严禁盲目读取大目录。找文件必须先 `ls`，精准定位后再读具体文件。
- 死循环熔断：如果尝试修复同一个 Bug 连续失败 3 次，必须立即停止操作并询问用户，严禁不断重试。
- [cite_start]响应风格：不要废话，保持直接和技术性，始终包含 task_progress 。

## 3. 技术栈与质量红线 (Phase F1)
- [cite_start]状态管理：必须使用 Riverpod （使用 @riverpod 注解）。
- [cite_start]路由系统：必须使用 GoRouter 。
- [cite_start]金融/支付安全：金额字段强制使用 `JsonNumConverter.toDouble`，严禁直接使用 double 计算 [cite: 2]。
- 代码规范：
  - [cite_start]函数必须控制在 50 行以内 [cite: 2]。
  - [cite_start]build() 方法内业务逻辑不超过 3 行 [cite: 2]。
  - [cite_start]禁止硬编码颜色/尺寸，使用生成的设计令牌 [cite: 2]。

## 4. 测试与提交流程
- [cite_start]提交流程：完成代码后必须通过 `fvm flutter analyze && fvm flutter test` [cite: 2]。警告(Warning)视同错误(Error)。
- [cite_start]新功能必须加 1 个 Unit + 1 个 Widget 测试，Bug 修复必须加回归测试 [cite: 2]。

## 5. 决策框架与越权禁止
- [cite_start]允许自主执行：明确的 Bug 修复、文档/UI 调整、依赖更新、代码格式化 。
- [cite_start]必须暂停询问用户：架构变更、新功能、数据库变更、性能优化、涉及金融/支付的逻辑修改 。